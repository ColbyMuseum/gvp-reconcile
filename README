gvp-reconcile: A command line tool for reconciling JSON data to the AAT.

A basic tool that loads a JSON file and reconciles fields to the Getty Vocabulary Program's Art and Architecture Thesaurus, prompts users to build a map fo terms and saves them to as JSON to a mapping file, and writes out the input JSON with fields added for the AAT's prefLabel term and the URL to the concept.
